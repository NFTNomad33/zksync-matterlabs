from zksync import Wallet, ZkSyncAPI
from zksync.types import ChangePubKey, Transfer

def main():
    # Replace these with your own values
    private_key = "your_private_key"
    sync_api_base_url = "https://api.zksync.io"

    # Connect to zkSync
    wallet = Wallet(private_key, sync_api_base_url)
    zk_sync_api = ZkSyncAPI(sync_api_base_url)

    # Get account information
    account_info = wallet.get_account_info()
    print("
